baseURL = "https://docs.roguerope.be/"
title = "Oh Bondage! Up Yours!"
enableRobotsTXT = true
# this example loads the theme as hugo module
# comment out line below, and uncomment the line after it if you prefer to load the theme normally
# theme = ["github.com/roguerope/compose"] # edit this if you'ld rather use a fork of this repon
theme = "compose"
enableGitInfo = true

enableDarkMode = true # set to false to disable darkmode by default # user will still have the option to use dark mode
defaultLightingMode = "dark" # other possible values: "dark", "light"
enforceDarkMode = true


# disableKinds = ["taxonomy", "taxonomyTerm"]

[outputs]
  home = ["HTML", "RSS","JSON"]